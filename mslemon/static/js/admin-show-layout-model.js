// Generated by CoffeeScript 1.6.3
(function() {
  $(document).ready(function() {
    var div, header_template, icon, input, label, renderable, span, strong, text;
    renderable = teacup.renderable;
    div = teacup.div;
    icon = teacup.i;
    strong = teacup.strong;
    span = teacup.span;
    label = teacup.label;
    input = teacup.input;
    text = teacup.text;
    header_template = renderable(function(content) {
      return div('.btn.btn-default.btn-xs', function() {
        return text(content);
      });
    });
    $('.edit-button').click(function() {
      var btnid, elist, id, oldtext;
      btnid = $(this).attr('id');
      elist = btnid.split('-');
      id = elist[1];
      oldtext = $('.header').text();
      if (oldtext === 'Ms. Lemon') {
        return $('.header').html(header_template('FOoo-' + id));
      } else {
        return $('.header').html(header_template('Ms. Lemon'));
      }
    });
    $('#attach-css-button').click(function() {
      var url;
      url = $(this).attr('href');
      window.location = url;
      return $(this).hide();
    });
    $('#attach-js-button').click(function() {
      var url;
      url = $(this).attr('href');
      window.location = url;
      return $(this).hide();
    });
    return $('.detach-button').click(function() {
      var btnid, ctype, detach_id, elist, url;
      btnid = $(this).attr('id');
      url = $(this).attr('href');
      elist = btnid.split('-');
      ctype = elist[1];
      detach_id = elist[2] + '-' + elist[3];
      $('header > h2').text(ctype + '-' + detach_id);
      return window.location = url;
    });
  });

}).call(this);
