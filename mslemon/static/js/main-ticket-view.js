// Generated by CoffeeScript 1.6.1
(function() {
  var loading_events, make_calendar;

  loading_events = function(bool) {
    if (bool) {
      $('#loading').show();
      return $('.fc-header').hide();
    } else {
      $('#loading').hide();
      return $('.fc-header').show();
    }
  };

  make_calendar = function(element, url, defaultview, firsthour) {
    element.fullCalendar('destroy');
    return element.fullCalendar({
      header: {
        left: 'month, agendaWeek, agendaDay',
        center: 'title'
      },
      theme: true,
      eventSources: [
        {
          url: url,
          color: '#8B8878'
        }
      ],
      editable: false,
      selectable: false,
      allDayDefault: false,
      eventColor: '#8B8878',
      defaultView: defaultview,
      loading: loading_events,
      firstHour: firsthour
    });
  };

  $(document).ready(function() {
    var rnow, thour;
    $('#loading').hide();
    rnow = new Date();
    thour = rnow.getHours() - 2;
    $('#ticket-list').hide();
    $('#ticket-calendar').fullCalendar({
      header: {
        left: 'month, agendaWeek, agendaDay',
        center: 'title'
      },
      theme: true,
      eventSources: [
        {
          url: $('#assignedUrl').val(),
          color: '#8B8878'
        }
      ],
      editable: false,
      selectable: false,
      allDayDefault: false,
      eventColor: '#8B8878',
      defaultView: $('#calendar_view_assigned').val(),
      firstHour: thour
    });
    $('#list-button').click(function() {
      var button, text;
      button = $('#list-button');
      $('#ticket-calendar').toggle();
      $('#ticket-list').toggle();
      text = button.text();
      button.text('sdfsdfsdf');
      if (text === 'List') {
        button.text('Calendar');
      }
      if (text === 'Calendar') {
        return button.text('List');
      }
    });
    return $('.query-button').click(function() {
      var button, calendar_content, defaultView, defaultViewID, element, hr, list_content, now, tkt_type, url, urlid, view, viewbutton;
      calendar_content = $('#ticket-calendar');
      list_content = $('#ticket-list');
      viewbutton = $('#list-button');
      view = viewbutton.text();
      button = $(this);
      tkt_type = $(this).attr('id').split('-')[0];
      defaultViewID = '#calendar_view_' + tkt_type;
      defaultView = $(defaultViewID).val();
      if (view === 'List') {
        urlid = '#' + tkt_type + 'Url';
        now = new Date();
        hr = now.getHours() - 2;
        url = $(urlid).val();
        element = calendar_content;
        return make_calendar(element, url, defaultView, hr);
      } else {
        urlid = '#ALL' + tkt_type + 'Url';
        url = $(urlid).val();
        return list_content.load(url, {}, function() {
          var title;
          title = tkt_type.slice(0, 1).toUpperCase() + tkt_type.slice(1);
          return $('.phonecalls-list-header').text(title + ' Phone Calls');
        });
      }
    });
  });

}).call(this);
